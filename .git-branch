#! /bin/bash
DIR=$(tmux display-message -p -F "#{pane_current_path}")
GIT_BRANCH=$(cd $DIR; git symbolic-ref HEAD 2>/dev/null | cut -d"/" -f 3)
if [ -n "$GIT_BRANCH" ]; then
    echo " git: ${GIT_BRANCH} "
fi
